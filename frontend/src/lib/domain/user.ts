export interface IUser {
  email: string;
  id: number;
  isDriver: boolean;
  organizations: any;
  phoneNumber: string;
  photoUrl: string | null;
  firstName: string;
  lastName: string;
  userId: number;
}
